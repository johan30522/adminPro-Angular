import { ImagenUrlPipe } from './imagen-url.pipe';

describe('ImagenUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
